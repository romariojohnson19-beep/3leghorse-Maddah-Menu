// imgui_draw.cpp - trimmed for embedding: draw list operations minimal
#include "imgui.h"
// Minimal implementation to satisfy ImGui::GetDrawData usage.
// In a full ImGui build this contains rendering primitives. For our purpose
// backends call ImGui::GetDrawData() which is mocked in imgui.h.
